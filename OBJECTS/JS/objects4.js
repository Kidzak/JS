"use strict";

//  function makeUser() {
    //  return {
    //  name: "John",
    //  ref: this
    //  };
    //  }
    //  let user1 = makeUser();
    //  alert( user1.ref.name );

    // In this code, 'user.ref' refer to global object, not the user object itself. 
    // 'user.ref.name' Throws an error because 'user.ref' corresponds to it.